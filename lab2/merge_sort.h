#ifndef MERGE_SORT_H
#define MERGE_SORT_H
#include <vector>

namespace sorts
{
    template<typename T>
    void merge_sort(std::vector<T>& arr);
}
namespace sorts
{
    template<typename T>
    void merge_sort(std::vector<T>& arr)
    {

    }
}
#endif // MERGE_SORT_H
